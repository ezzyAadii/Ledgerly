import {NavLink,Outlet,useLocation} from 'react-router-dom';import {LayoutDashboard,ArrowLeftRight,WalletCards,Target,Repeat2,User,LogOut,Moon,Sun,Menu,X,Landmark} from 'lucide-react';import {useState} from 'react';import {useAuth} from '../context/AuthContext';import {useTheme} from '../context/ThemeContext';
const links=[['/dashboard','Overview',LayoutDashboard],['/transactions','Transactions',ArrowLeftRight],['/budgets','Budgets',WalletCards],['/goals','Savings goals',Target],['/recurring','Recurring',Repeat2],['/profile','Profile',User]];
export default function Layout(){const [open,setOpen]=useState(false),{user,logout}=useAuth(),{dark,setDark}=useTheme(),loc=useLocation();const title=links.find(x=>loc.pathname.startsWith(x[0]))?.[1]||'Ledgerly';return <div className="min-h-screen lg:grid lg:grid-cols-[248px_1fr]"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r bg-white p-4 transition-transform dark:bg-zinc-950 lg:static lg:w-auto lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="mb-8 flex items-center justify-between px-2"><NavLink to="/dashboard" className="flex items-center gap-2 text-xl font-bold"><span className="rounded-lg bg-amber-500 p-1.5 text-zinc-950"><Landmark size={20}/></span>Ledgerly</NavLink><button className="lg:hidden" onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button></div><nav className="space-y-1">{links.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${isActive?'bg-amber-500/15 text-amber-700 dark:text-amber-400':'muted hover:bg-zinc-100 dark:hover:bg-zinc-900'}`}><Icon size={18}/>{label}</NavLink>)}</nav><button onClick={logout} className="absolute bottom-5 left-4 right-4 flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-rose-600 hover:bg-rose-50 dark:hover:bg-rose-950/30"><LogOut size={18}/>Sign out</button></aside>{open&&<button className="fixed inset-0 z-30 bg-black/40 lg:hidden" onClick={()=>setOpen(false)} aria-label="Close navigation"/>}<div className="min-w-0"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b bg-[#FAFAFA]/90 px-4 backdrop-blur dark:bg-[#09090B]/90 md:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={()=>setOpen(true)} aria-label="Open menu"><Menu/></button><h1 className="text-lg font-semibold">{title}</h1></div><div className="flex items-center gap-3"><button onClick={()=>setDark(!dark)} className="rounded-lg border p-2" aria-label="Toggle dark mode">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><div className="hidden text-right sm:block"><p className="text-sm font-medium">{user?.name}</p><p className="text-xs muted">{user?.email}</p></div>{user?.avatarUrl?<img src={user.avatarUrl} className="h-9 w-9 rounded-full" referrerPolicy="no-referrer"/>:<div className="grid h-9 w-9 place-items-center rounded-full bg-amber-500 font-semibold text-zinc-950">{user?.name?.[0]?.toUpperCase()}</div>}</div></header><main className="mx-auto max-w-7xl p-4 md:p-8"><Outlet/></main></div></div>}
