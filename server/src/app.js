import express from 'express';import helmet from 'helmet';import cors from 'cors';import {rateLimit} from 'express-rate-limit';import config from './config.js';import {authenticate} from './middleware/auth.js';import {notFound,errorHandler} from './middleware/errors.js';import auth from './routes/auth.js';import transactions from './routes/transactions.js';import dashboard from './routes/dashboard.js';import {budgets,goals,recurring} from './routes/resources.js';
export function createApp(){const app=express();app.disable('x-powered-by');app.set('trust proxy',1);app.use(helmet());app.use(cors({origin:config.clientUrl.split(',').map(x=>x.trim()),methods:['GET','POST','PUT','DELETE'],allowedHeaders:['Content-Type','Authorization'],maxAge:86400}));app.use(express.json({limit:'100kb'}));app.use('/api/auth',rateLimit({windowMs:15*60*1000,limit:100,standardHeaders:'draft-7',legacyHeaders:false}),auth);app.get('/api/health',(_q,s)=>s.json({success:true,data:{status:'ok'}}));app.use('/api',authenticate);app.use('/api/transactions',transactions);app.use('/api/budgets',budgets);app.use('/api/goals',goals);app.use('/api/recurring',recurring);app.use('/api/dashboard',dashboard);app.use(notFound);app.use(errorHandler);return app;}
